{
  "name": "tm-fetcher",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "express": "^4.19.2",
    "playwright": "^1.47.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
